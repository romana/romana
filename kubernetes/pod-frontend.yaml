apiVersion: v1
kind: Pod
metadata:
  name: nginx-frontend
  namespace: tenantA
  labels:
    app: nginx
    segment: frontend
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
